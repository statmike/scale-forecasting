"""Consensus across base models — calculated + learned — pure logic + SQL builders (CONTRACTS §6).

Owned by BUILD step 6a. Public surface: ``build_ensemble_sql``, ``fit_learned``.
"""

from __future__ import annotations


def build_ensemble_sql(cfg: object) -> str:  # pragma: no cover - stub
    raise NotImplementedError("ensembler.build_ensemble_sql — BUILD step 6a")
