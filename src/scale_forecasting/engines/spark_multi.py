"""Spark method B — one serverless batch per model family (CONTRACTS §6, DESIGN §6).

Owned by BUILD step B2. Public surface: ``run(cfg) -> None``.
"""

from __future__ import annotations


def run(cfg: object) -> None:  # pragma: no cover - stub, see BUILD B2
    raise NotImplementedError("engines.spark_multi.run — BUILD step B2")
