"""BigQuery-native models — SQL template runner for ARIMA_PLUS / AI.FORECAST (CONTRACTS §5).

Owned by BUILD step B3. Public surface: ``run(cfg, models) -> None``.
"""

from __future__ import annotations


def run(cfg: object, models: object) -> None:  # pragma: no cover - stub, see BUILD B3
    raise NotImplementedError("engines.bigquery_engine.run — BUILD step B3")
