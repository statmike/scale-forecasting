"""Orchestrate one run end-to-end; CLI entrypoint (CONTRACTS §6).

Owned by BUILD Arc B. Public surface: ``run(cfg) -> run_id`` and
``python -m scale_forecasting.main --config ... [--dry-run]``.
"""

from __future__ import annotations


def run(cfg: object) -> str:  # pragma: no cover - stub, see BUILD Arc B
    raise NotImplementedError("main.run — BUILD Arc B")
