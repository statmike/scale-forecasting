"""Split a run's model list by runtime (CONTRACTS §6).

Owned by BUILD step B3. Public surface: ``split_by_runtime(cfg) -> (python, bq)``.
"""

from __future__ import annotations


def split_by_runtime(cfg: object) -> object:  # pragma: no cover - stub, see BUILD B3
    raise NotImplementedError("router.split_by_runtime — BUILD step B3")
