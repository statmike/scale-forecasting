"""The model interface every model file implements (CONTRACTS §1).

Owned by BUILD step 2.2. Public surface: ``BaseModel``, ``ModelContext``, ``register``.
"""

from __future__ import annotations


class BaseModel:  # pragma: no cover - stub, see BUILD 2.2
    """ABC defined in BUILD step 2.2 (fit / predict / params / search_space)."""
