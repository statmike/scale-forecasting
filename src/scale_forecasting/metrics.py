"""Forecast metric panel — pure (CONTRACTS §2.3, DESIGN §5.1).

Owned by BUILD step 2.1. Public surface: ``compute_metrics``.
"""

from __future__ import annotations


def compute_metrics(*args: object, **kwargs: object) -> dict[str, float]:  # pragma: no cover - stub
    raise NotImplementedError("metrics.compute_metrics — BUILD step 2.1")
