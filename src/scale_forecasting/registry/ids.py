"""Deterministic identifiers (CONTRACTS §3, §6).

Owned by BUILD step 1.2. Public surface: ``make_run_id``, ``make_model_hash``.
"""

from __future__ import annotations


def make_run_id(cfg: object) -> str:  # pragma: no cover - stub, see BUILD 1.2
    raise NotImplementedError("registry.ids.make_run_id — BUILD step 1.2")
