"""Cross-validation folds + out-of-fold capture — pure (CONTRACTS §2.2).

Owned by BUILD step 2.4. Public surface: ``make_folds``, ``backtest_cell``.
"""

from __future__ import annotations


def make_folds(n: int, cfg: object) -> object:  # pragma: no cover - stub
    raise NotImplementedError("backtest.make_folds — BUILD step 2.4")
